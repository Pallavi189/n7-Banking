import Button from "./ui/Button";

function Frame36({ className = "" }) {
  return (
    <Button preset="outlineRequest" className={className} icon={false}>
      REQUEST DEMO
    </Button>
  );
}

export default Frame36;
